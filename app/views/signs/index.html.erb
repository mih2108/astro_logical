<% if user_signed_in? %>
	<div><span>Hello <%= current_user.email %> </span>
<% else %>
<% end %>
<% @signs.each do |sign| %>
	     		<div class="col-mod-3">
				     <div class="sign_image"> 
			        	<p><%= link_to (asset_path sign.image_1), sign_path(sign) %></p>
			       	 </div>	
			    </div>
		
 <% end %>