<h1></h1>
<% @signs.in_groups_of(6) do |sign| %>
	     <div class="row">
	     	<% sign.compact.each do |sign| %>
	     		<div class="col-mod-2">
				     <div class="sign_image"> 
			        	<p><%= link_to (image_tag sign.image_1), sign_path(sign) %></p>
			       	 </div>	
			    </div>
			<% end %>
       	 </div>
 <% end %>


